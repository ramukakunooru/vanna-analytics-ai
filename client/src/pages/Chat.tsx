import ChatInterface from "@/components/ChatInterface";

export default function Chat() {
  return (
    <div className="h-[calc(100vh-4rem)] w-full max-w-7xl mx-auto">
      <ChatInterface />
    </div>
  );
}
